# Extras

These files are not part of the default OpenCode starter surface.

Use them only after a project has the required tools and the user explicitly
wants that workflow.

- `tool-dependent-skills/frontend-app-builder`: image-led concept workflow that
  assumes image generation, screenshot viewing, and browser verification.

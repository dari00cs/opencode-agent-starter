---
description: Plan or review frontend work with taste, accessibility, and browser checks.
---

Use OpenCode-safe frontend skills first: `frontend-skill`, `taste-skill`,
`redesign-skill`, `react-best-practices`, and `shadcn` when they match.
Use style flavors like `minimalist-skill`, `soft-skill`, `brutalist-skill`,
or `gpt-tasteskill` only when the request clearly calls for that direction.

Do not reach into `extras/tool-dependent-skills/` unless the user explicitly
asks and the session has the required image, screenshot, and browser tooling.

Request:

$ARGUMENTS

Inspect the existing UI patterns before changing anything. Plan for responsive
layout, accessibility, empty/loading/error states, and browser verification.

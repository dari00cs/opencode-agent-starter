# Advanced And Optional

Specialized skills that are useful later, not day one.

- `stitch-skill`: generate a Google Stitch-oriented `DESIGN.md` or semantic
  design-system spec. It is not for normal frontend coding.

Tool-dependent workflows that should not be active by default live in `extras/`.
